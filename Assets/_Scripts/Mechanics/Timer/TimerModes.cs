namespace Timer
{
    public enum TimerMode
    {
        CountDown,
        CountUp
    }

    public enum ShowTimerMode
    {
        Seconds,
        Seconds_Milliseconds,
        Minutes,
        Minutes_Seconds,
        Hours,
        Hours_Minutes
    }
}
